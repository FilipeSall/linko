import styles from "./page.module.scss";

function page() {
  return (
    <main className={styles.content}>
      <form className={styles.form}>
        
      </form>
    </main>
  );
}

export default page;
